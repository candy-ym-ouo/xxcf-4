# 旧物维修履历（Repair Log）项目文档

> 为相机、收音机、家具、乐器等旧物建立数字档案，完整记录损坏部位、维修过程、使用材料与维修前后照片。
> 前端 React，后端 Node.js，支持附件管理、保养提醒与家庭共享。

---

## 1. 项目背景与目标

老物件的维修知识往往散落在记忆、纸条和手机相册里：三年前给禄来相机换的是哪种遮光皮革、上次给吉他调整琴颈是什么时候、五斗柜的榫头用的什么胶——时间一长全都记不清，下次维修只能从头摸索，转手或传给家人时也说不清来历。

本项目要解决的就是这件事：把一次维修从"损坏—诊断—处理—用料—结果"完整地记下来，附上照片，并在需要复检或保养时主动提醒。

**产品目标**

1. 每个物件有一份可持续追加的档案，维修履历按时间线沉淀。
2. 记录足够结构化，能回答"用了什么材料、花了多少钱、谁修的、效果如何"。
3. 照片是一等公民，支持维修前/中/后分组对比。
4. 保养类事项（清洁、上油、换弦、除湿）能定期提醒。
5. 家庭成员可共享同一份档案库，各自补充记录。

**明确不做的事（首版）**

- 不做维修知识社区、不做公开分享广场。
- 不做维修师傅撮合与交易。
- 不做多租户 SaaS 商业化，先服务单个家庭。

---

## 2. 目标用户与典型场景

| 用户 | 关注点 |
| --- | --- |
| 器物收藏爱好者 | 履历完整度、材料与工艺细节、可导出留存 |
| 家庭日常使用者 | 记录简单、提醒及时、手机上能拍完就存 |
| 家庭共享成员 | 看得到别人修了什么，能补充自己的记录 |

**场景一：一次相机维修**
海鸥 4A 的取景器起雾。用户新建维修记录，选中物件"海鸥 4A"，标注损坏部位为"取景器/毛玻璃"，先拍三张维修前照片，写下诊断"密封海绵老化导致进潮"，处理过程分步记录拆解、清洁、更换遮光海绵，材料清单里加一条"1mm 自粘遮光海绵，¥18，某宝"，完成后拍两张维修后照片，状态标记为"已修复"。

**场景二：周期保养**
吉他档案里设置"每 3 个月换弦并上指板油"的循环提醒。到期后系统在提醒中心和邮件里通知，用户点"完成并记录"，直接生成一条保养类型的履历。

**场景三：家庭共享**
父亲把老收音机的档案库共享给家庭空间，儿子作为编辑者补充了自己更换电解电容的记录，家庭成员都能看到这台机器完整的维修史。

---

## 3. 核心概念与数据模型

### 3.1 概念关系

```
Household（家庭空间）
  └── Member（成员，带角色）
  └── Item（物件：相机/收音机/家具/乐器/其他）
        ├── DamagePoint（损坏部位，可复用于多次维修）
        ├── RepairRecord（维修履历，一次维修=一条）
        │     ├── RepairStep（处理步骤，有序）
        │     ├── MaterialUsage（使用材料及用量、成本）
        │     └── Attachment（照片/票据/说明书，带 before/after 分组）
        └── Reminder（提醒：一次性或周期性）
```

### 3.2 主要实体字段

**Item（物件）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| household_id | uuid | 所属家庭空间 |
| name | string | 物件名称，如"海鸥 4A 双反" |
| category | enum | camera / radio / furniture / instrument / other |
| brand / model | string | 品牌、型号 |
| year_made | int? | 生产年份 |
| acquired_at | date? | 入手时间 |
| acquired_from | string? | 来源（继承、二手市场、店铺） |
| condition | enum | excellent / good / fair / broken |
| cover_attachment_id | uuid? | 封面图 |
| description | text | 自述、故事、来历 |
| tags | string[] | 自由标签 |
| archived | bool | 是否归档（转手/报废后保留履历） |

**DamagePoint（损坏部位）**

| 字段 | 说明 |
| --- | --- |
| item_id | 所属物件 |
| name | 部位名称，如"取景器"、"左侧抽屉滑轨"、"第 5 弦品丝" |
| region | 可选，结构化区域标记（用于后续在照片上打点） |
| severity | minor / moderate / severe |
| status | open / repairing / fixed / wontfix |
| first_noticed_at | 首次发现时间 |

**RepairRecord（维修履历）**

| 字段 | 说明 |
| --- | --- |
| item_id | 所属物件 |
| type | repair 维修 / maintenance 保养 / restoration 修复 / inspection 检查 |
| title | 一句话概括，如"更换取景器遮光海绵" |
| damage_point_ids | 本次涉及的损坏部位 |
| diagnosis | 故障诊断（文本） |
| started_at / finished_at | 起止时间 |
| performed_by | self 自己 / family 家人 / shop 店铺 |
| shop_name / shop_contact | 委外时的店铺信息 |
| labor_cost | 工时费 |
| result | fixed / partial / failed / observing |
| notes | 结果说明、后续注意事项 |
| created_by | 记录人 |

**RepairStep（处理步骤）**：`record_id, order, content, duration_min?, attachment_ids[]`

**MaterialUsage（使用材料）**：`record_id, name, spec, quantity, unit, unit_price, total_cost, source, note`
> 材料同时写入家庭级"材料库"做名称联想，下次输入"遮光海绵"可直接带出规格与价格。

**Attachment（附件）**

| 字段 | 说明 |
| --- | --- |
| owner_type / owner_id | 挂载对象（item / record / step） |
| phase | before / during / after / doc（票据、说明书） |
| file_key | 对象存储 key |
| mime / size / width / height | 元信息 |
| thumb_key | 缩略图 key |
| caption | 图注 |
| taken_at | 拍摄时间（读 EXIF） |

**Reminder（提醒）**

| 字段 | 说明 |
| --- | --- |
| item_id | 关联物件 |
| title | 如"吉他换弦" |
| rule | once / every_n_days / monthly / quarterly / yearly |
| next_fire_at | 下次触发时间 |
| channels | in_app / email |
| status | active / paused / done |
| auto_create_record | 完成时是否自动生成一条保养履历 |

**Household / Member（家庭共享）**

- `Household`：家庭空间，档案库的归属单位。
- `Member`：`household_id, user_id, role`，角色为 `owner`（唯一，可删空间与管成员）、`editor`（增删改自己的记录、读全部）、`viewer`（只读）。
- 邀请通过一次性邀请码或邮件邀请链接完成，链接 7 天过期。

---

## 4. 功能清单

### 4.1 物件档案

- 物件列表：网格卡片（封面图 + 名称 + 类别 + 最近维修时间），支持按类别、状态、标签筛选与关键词搜索。
- 物件详情：基本信息、损坏部位清单、维修履历时间线、照片墙、提醒、累计花费统计。
- 新建/编辑物件，支持归档而非删除（保留履历）。

### 4.2 维修履历

- 时间线视图：按时间倒序，每条显示类型标签、标题、涉及部位、结果、缩略图。
- 记录详情：诊断、分步过程、材料表（自动汇总成本）、前后照片对比。
- **前后对比组件**：同一部位的 before / after 图支持左右滑块对比与并排查看。
- 草稿态：维修进行中可先存草稿，边修边补，完成后再提交。

### 4.3 附件管理

- 支持 JPEG / PNG / HEIC / WebP / PDF，单文件上限 20MB，单条记录最多 30 个附件。
- 客户端压缩后上传，服务端生成 400px 缩略图与 1600px 展示图，原图另存。
- 上传走**预签名直传**：前端向后端换取上传凭证，直传对象存储，回调登记元信息。
- 读取走带签名的临时 URL（默认 15 分钟有效），避免附件被匿名访问。
- 自动读取 EXIF 拍摄时间与方向，按拍摄时间排序。

### 4.4 提醒

- 物件维度创建提醒，支持一次性与周期性。
- 服务端定时任务每小时扫描到期提醒，写入站内提醒中心并按配置发邮件。
- 提醒可"完成"、"顺延"、"暂停"；完成时可一键生成保养履历。

### 4.5 家庭共享

- 一个用户可属于多个家庭空间，顶部切换当前空间。
- 邀请成员、变更角色、移除成员（仅 owner）。
- 记录上显示创建人，avoid 谁改了什么说不清。
- 操作审计日志（谁在什么时候改了哪条记录）。

### 4.6 统计与导出

- 单个物件：累计维修次数、累计花费、材料 Top5。
- 家庭维度：本年度维修花费、各类别物件数量分布。
- 导出：单物件履历导出为 PDF（含照片），全量数据导出为 JSON + 附件压缩包。

---

## 5. 技术架构

### 5.1 技术选型

**前端**

| 项 | 选型 | 理由 |
| --- | --- | --- |
| 框架 | React 18 + TypeScript | 团队熟悉，生态成熟 |
| 构建 | Vite | 启动与 HMR 快 |
| 路由 | React Router v6 | 标准方案 |
| 服务端状态 | TanStack Query | 缓存、失效、乐观更新开箱即用 |
| 表单 | React Hook Form + Zod | 与后端共用一套校验 schema |
| UI | Tailwind CSS + shadcn/ui | 可控、无重型依赖 |
| 图片 | react-compare-slider + 自研懒加载 | 前后对比 |

**后端**

| 项 | 选型 | 理由 |
| --- | --- | --- |
| 运行时 | Node.js 20 LTS + TypeScript | 与前端同语言 |
| 框架 | Fastify | 性能好，schema 校验与 JSON Schema 一体 |
| ORM | Prisma | 迁移与类型安全 |
| 数据库 | PostgreSQL 15 | 关系明确，JSONB 存扩展字段 |
| 对象存储 | S3 兼容（本地开发用 MinIO） | 预签名直传 |
| 任务调度 | node-cron + 数据库任务表 | 提醒扫描，量小无需引入队列 |
| 图片处理 | sharp | 缩略图、EXIF |
| 鉴权 | JWT（access 15min + refresh 30d，httpOnly Cookie） | 无状态、易扩展 |
| 邮件 | Nodemailer + SMTP | 提醒与邀请 |
| 日志 | pino | Fastify 原生 |

### 5.2 系统结构

```
┌──────────────┐    HTTPS/JSON     ┌─────────────────┐
│  React SPA   │ ────────────────▶ │  Fastify API    │
│  (Vite)      │ ◀──────────────── │  (Node.js 20)   │
└──────┬───────┘                   └────┬───────┬────┘
       │  预签名 PUT / GET               │       │
       ▼                                 ▼       ▼
┌──────────────┐               ┌────────────┐ ┌──────────┐
│ 对象存储 S3   │◀──────────────│ PostgreSQL │ │ 定时任务  │
│ (原图/缩略图) │   元信息登记   │            │ │ 提醒扫描  │
└──────────────┘               └────────────┘ └────┬─────┘
                                                    │ SMTP
                                                    ▼
                                               邮件通知
```

### 5.3 目录结构

```
xxcf-1/
├── 项目文档.md
├── package.json                # pnpm workspace 根
├── packages/
│   └── shared/                 # 前后端共用类型与 Zod schema
│       └── src/{types,schemas}
├── apps/
│   ├── web/                    # React 前端
│   │   └── src/
│   │       ├── pages/          # ItemList / ItemDetail / RecordEditor / Reminders / Household
│   │       ├── components/     # PhotoCompare / Timeline / MaterialTable / Uploader
│   │       ├── features/       # 按领域聚合 hooks + api
│   │       ├── lib/            # http client, auth, format
│   │       └── routes.tsx
│   └── api/                    # Node.js 后端
│       └── src/
│           ├── modules/        # auth / household / item / record / attachment / reminder
│           │   └── item/{routes,service,repository}.ts
│           ├── plugins/        # auth, error-handler, storage, mailer
│           ├── jobs/           # reminder-scanner.ts
│           ├── db/             # prisma schema & migrations
│           └── server.ts
└── docker-compose.yml          # postgres + minio + mailhog
```

分层约定：`routes` 只做参数校验与响应装配，`service` 承载业务规则与权限判断，`repository` 只碰数据库。权限检查统一在 service 入口做，不散落在路由里。

---

## 6. API 设计

统一前缀 `/api/v1`，认证走 httpOnly Cookie 中的 access token，所有业务接口要求带 `X-Household-Id` 指明当前家庭空间。

### 6.1 认证与家庭空间

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/auth/register` | 注册 |
| POST | `/auth/login` | 登录，下发 access/refresh Cookie |
| POST | `/auth/refresh` | 刷新 access token |
| POST | `/auth/logout` | 登出 |
| GET | `/households` | 我加入的家庭空间列表 |
| POST | `/households` | 创建空间 |
| POST | `/households/:id/invites` | 生成邀请（owner） |
| POST | `/invites/:code/accept` | 接受邀请 |
| GET | `/households/:id/members` | 成员列表 |
| PATCH | `/households/:id/members/:uid` | 改角色（owner） |
| DELETE | `/households/:id/members/:uid` | 移除成员（owner） |

### 6.2 物件与履历

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/items` | 列表，支持 `category`、`q`、`tag`、`archived`、`page`、`size` |
| POST | `/items` | 新建物件 |
| GET | `/items/:id` | 详情（含部位、最近 5 条履历、提醒） |
| PATCH | `/items/:id` | 更新 |
| POST | `/items/:id/archive` | 归档 |
| GET | `/items/:id/damage-points` | 部位列表 |
| POST | `/items/:id/damage-points` | 新增部位 |
| PATCH | `/damage-points/:id` | 更新部位状态 |
| GET | `/items/:id/records` | 履历时间线（分页） |
| POST | `/items/:id/records` | 新建履历（可为 draft） |
| GET | `/records/:id` | 履历详情（含步骤、材料、附件） |
| PATCH | `/records/:id` | 更新（含步骤与材料整体替换） |
| DELETE | `/records/:id` | 删除（软删，30 天可恢复） |
| GET | `/items/:id/stats` | 单物件统计 |

### 6.3 附件

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/attachments/presign` | 入参 `{filename, mime, size, ownerType, ownerId, phase}`，返回 `{uploadUrl, fileKey, expiresIn}` |
| POST | `/attachments/complete` | 直传完成后登记，服务端异步生成缩略图 |
| GET | `/attachments/:id/url` | 换取临时读取 URL |
| PATCH | `/attachments/:id` | 改 caption / phase / 排序 |
| DELETE | `/attachments/:id` | 删除（同时清理对象存储） |

### 6.4 提醒

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/reminders` | 当前空间提醒，`status`、`dueBefore` 过滤 |
| POST | `/items/:id/reminders` | 创建 |
| PATCH | `/reminders/:id` | 修改规则 / 暂停 |
| POST | `/reminders/:id/complete` | 完成，可选 `createRecord: true` |
| POST | `/reminders/:id/snooze` | 顺延 N 天 |

### 6.5 约定

- 成功：`200/201`，响应体即资源对象；列表返回 `{ data: [], page, size, total }`。
- 失败：统一 `{ error: { code, message, details? } }`，`code` 为业务码如 `ITEM_NOT_FOUND`、`FORBIDDEN_ROLE`、`FILE_TOO_LARGE`。
- 分页默认 `size=20`，上限 100。
- 所有写操作幂等键可选（`Idempotency-Key` 头），用于弱网重试。

---

## 7. 前端页面设计

| 页面 | 路由 | 要点 |
| --- | --- | --- |
| 登录/注册 | `/login` `/register` | 简单表单 |
| 物件列表（首页） | `/items` | 卡片网格 + 类别侧栏 + 搜索；空态引导"添加第一件物品" |
| 物件详情 | `/items/:id` | 顶部信息卡，标签页：履历时间线 / 损坏部位 / 照片墙 / 提醒 / 统计 |
| 履历编辑器 | `/items/:id/records/new`、`/records/:id/edit` | 分区表单：基本信息 → 涉及部位 → 诊断 → 处理步骤（可拖拽排序）→ 材料表 → 照片（before/during/after 三栏拖放） |
| 履历详情 | `/records/:id` | 只读展示 + 前后对比滑块 + 成本汇总 |
| 提醒中心 | `/reminders` | 按到期时间分组：已逾期 / 本周 / 以后 |
| 家庭空间 | `/household` | 成员管理、邀请码、审计日志 |
| 设置 | `/settings` | 个人信息、通知偏好、数据导出 |

**交互重点**

- 履历编辑器全程本地草稿自动保存（每 5 秒写 IndexedDB），断网不丢内容。
- 照片上传并发 3 个，显示单文件进度，失败可单独重试。
- 移动端优先：列表单列、编辑器分步，拍照直接调用 `<input capture>`。
- 前后对比在移动端退化为上下滑动切换。

---

## 8. 非功能需求

| 维度 | 要求 |
| --- | --- |
| 性能 | 列表接口 P95 < 300ms；首屏 LCP < 2.5s（4G）；缩略图懒加载 |
| 容量 | 单家庭 500 物件、5000 条履历、20GB 附件内不降级 |
| 安全 | 附件不可匿名访问；密码 argon2id；接口按 household 隔离，每次查询强制带 household_id 条件 |
| 隐私 | 数据仅在家庭空间内可见，无公开分享入口；导出由用户自主触发 |
| 可用性 | 附件上传失败可断点重试；提醒任务失败自动重试 3 次并记录 |
| 备份 | 数据库每日全量备份保留 14 天；对象存储开启版本控制 |
| 可访问性 | 关键操作可键盘完成，图片有 alt（用 caption 兜底） |
| 兼容 | Chrome/Safari/Edge 近两个大版本，iOS Safari 16+ |

---

## 9. 开发计划

| 阶段 | 内容 | 产出 |
| --- | --- | --- |
| M0 基础设施（3 天） | monorepo、docker-compose、Prisma schema、CI | 本地一键起环境 |
| M1 认证与物件（1 周） | 注册登录、家庭空间、物件 CRUD、列表与详情 | 能建档 |
| M2 履历与附件（1.5 周） | 履历编辑器、步骤/材料、预签名上传、缩略图、前后对比 | 核心闭环可用 |
| M3 提醒（4 天） | 提醒 CRUD、定时扫描、站内 + 邮件、完成生成履历 | 保养提醒可用 |
| M4 共享与统计（1 周） | 邀请、角色权限、审计日志、统计与导出 | 家庭共享可用 |
| M5 打磨（1 周） | 移动端适配、空态与错误态、性能优化、E2E 用例 | 可发布 |

**验收标准（MVP）**

1. 能创建 4 类物件的档案并上传封面。
2. 能完整记录一次维修：部位、诊断、≥3 个步骤、≥2 条材料、前后各 ≥1 张照片，且详情页能对比查看。
3. 能设置季度提醒，到期收到站内与邮件通知，一键生成保养履历。
4. 能邀请第二个账号作为 editor 加入并补充记录，viewer 无法编辑。
5. 能导出单物件 PDF 履历。

---

## 10. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 附件体积增长快，存储成本上升 | 中 | 客户端压缩 + 原图可选保留；提供批量清理与容量看板 |
| HEIC 在部分浏览器不能直接预览 | 中 | 服务端统一转 WebP 作为展示图，原图保留 |
| 履历编辑器表单过重，用户放弃填写 | 高 | 只有标题必填，其余全可选；提供"快速记一笔"简版入口 |
| 家庭权限模型过早复杂化 | 中 | 首版只做 owner/editor/viewer 三角色，不做字段级权限 |
| 定时提醒漏发（进程重启） | 中 | 提醒扫描基于数据库 `next_fire_at` 而非内存定时器，重启后自动补发逾期项 |
| 单体 Node 进程处理图片阻塞事件循环 | 中 | sharp 放 worker_threads，或缩略图生成走独立轻量 worker |

---

## 11. 后续可扩展方向

- 在物件照片上直接打点标注损坏位置（坐标存 `DamagePoint.region`）。
- 材料库联动价格记录，生成"这台机器这些年花了多少钱"的报告。
- 支持导入既有相册，按 EXIF 时间自动归入对应履历。
- 物件转手时生成一份可分享的只读履历链接（带过期时间）。
- 离线优先（PWA + 后台同步），在没有信号的旧货市场也能记录。
